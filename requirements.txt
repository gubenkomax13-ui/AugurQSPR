# Streamlit Cloud may install either requirements.txt or environment.yml depending
# on the build path. Keep the online scientific stack here so the app can start
# even when conda dependencies are not applied.
-r requirements-online.txt
